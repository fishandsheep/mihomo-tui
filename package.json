{
  "name": "mihomo-tui-workspace",
  "version": "0.0.0-dev",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build:binaries": "bun scripts/build-binaries.mjs",
    "build:packages": "bun scripts/build-packages.mjs",
    "publish:packages": "bun scripts/publish-packages.mjs",
    "prepare:release": "bun scripts/prepare-release.mjs",
    "test:launchers": "bun scripts/test-package-launchers.mjs"
  }
}
